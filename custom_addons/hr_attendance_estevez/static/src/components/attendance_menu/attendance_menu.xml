<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-inherit="hr_attendance.attendance_menu" t-inherit-mode="extension" priority="30">
        <!-- Reemplaza el botón que contiene el icono fa-circle -->
        <xpath expr="//button[i[contains(@class, 'fa-circle')]]" position="replace">
            <button type="button"
                    class="btn btn-primary btn-sm o_attendance_systray_button o-dropdown dropdown-toggle dropdown"
                    aria-expanded="false">
                <i t-if="!this.state.checkedIn" class="fa fa-times text-danger" style="margin-right:4px;"/>
                <i t-if="this.state.checkedIn" class="fa fa-check text-success" style="margin-right:4px;"/>
                Asistencia
            </button>
        </xpath>

        <!-- Reemplaza TODO el contenedor con tu propia lógica -->
        <xpath expr="//Dropdown//t[@t-set-slot='content']//div[hasclass('o_att_menu_container')]" position="replace">
            <div class="o_att_menu_container d-flex flex-column gap-3">
                
                <!-- 1) Antes del primer checkin -->
                <div t-if="this.state.currentMoment === 'before_first_checkin'"
                     class="estevez-moment before-first d-flex flex-column align-items-start p-3 mb-3 rounded bg-light border">
                    <h5 class="mb-2 text-primary">Bienvenido 👋</h5>
                    <p class="mb-3 text-muted">
                        Todavía no has iniciado tu jornada.  
                        Pulsa el botón para hacer <strong>Check in</strong>.
                    </p>
                    <button t-on-click="() => this.signInOut()"
                            class="btn btn-success w-100">
                        <i class="fa fa-sign-in me-1"/> Iniciar jornada
                    </button>
                </div>

                <!-- Por ahora solo el primer momento -->
                <!-- Aquí luego agregamos in_journey y after_check_out -->

            </div>
        </xpath>


    </t>
</templates>